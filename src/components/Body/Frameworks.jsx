import React from 'react';
import 'materialize-css';
import 'react-materialize';
import './Frameworks.css';

export default function Frameworks() {
    return (
        <div className="container">
            <h5 className="col s12 white-text flow-text">Javascript || Node JS || jQuery || CSS || HTML5 || MySQL || Mongo DB || React || React Native</h5> 
            <p className="frameText">A modern developer familiar with several front-end frameworks such as Bulma, Bootstrap and Materialize. Dean is also skilled in back-end design and implementation. He has managed his own websites hosted on both remote, and on-premise hardware, and is a skilled network engineer.</p>
        </div>
    )
}
